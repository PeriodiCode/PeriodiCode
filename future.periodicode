#################################################
# FUTURE
# This file is for stockpiling ideas
# that might be supported in the future version.
#################################################

# variable
$three = 3;
$five = 5;
@assert_eq($three + $five, 8);

# Recurring continued fraction
$sqrt3 = [1; @rep{ 1, 2 }];
@assert_eq($sqrt3 * $sqrt3, 3);
